

name: "Continuous Integration"

on:
  pull_request:
    branches:
      - "*.x"
  push:
    branches:
      - "*.x"

jobs:
  phpunit:
    name: "PHPUnit"
    uses: "greg0ire/.github/.github/workflows/continuous-integration.yml@upgrade-deps"
    with:
      php-versions: '["8.1"]'
