
name: "Static Analysis"

on:
  pull_request:
    branches:
      - "*.x"
  push:
    branches:
      - "*.x"

jobs:
  static-analysis:
    name: "Static Analysis"
    uses: "greg0ire/doctrine-dot-github/.github/workflows/static-analysis.yml@dev-file"
